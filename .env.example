# StudyArena — environment
# Leave these blank to run the app in offline demo mode (local persistence).
# Fill them in to connect a real Supabase backend (see supabase/schema.sql).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
